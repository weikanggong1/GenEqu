# Synthetic examples

Every row in this directory was generated by the packaged equations. No real participant records are included. `manifest.json` records generator names, seeds, and sample counts. These small files demonstrate data formats; they are not the full training cohorts used in the manuscript. Clinical CSVs contain predictors followed by one binary outcome. Brain and protein NPZ keys are described in their generator READMEs.
